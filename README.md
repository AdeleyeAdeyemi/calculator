#Calculator App

A simple Flask-based calculator web application containerized with Docker and deployed through a Jenkins CI/CD pipeline.


# Features
- Basic calculator web UI (addition, subtraction, multiplication, division).
- Flask backend (Python 3.10).
- Containerized with **Docker & Docker Compose**.
- Automated build and deployment with **Jenkins pipeline**.
- Supports Selenium tests (for UI testing).

